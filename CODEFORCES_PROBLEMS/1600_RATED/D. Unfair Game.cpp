/*-------------------------------------------------------------
   NEVER GIVE UP THE SOLUTION MAY BE JUST AROUND THE CORNER
---------------------------------------------------------------*/

#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

/*
bob is guarted to win
bob chooses power of 2

intiial state = (2^d) d>=0

two moves either -1 or /2


to be divisible by 2^x  bits shoulbe 1000.. number of trailing zeroes==x
so max a can be 10000.. x times


*/

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;
    while (t--)
    {
        // Your code here
        ll n, k;
        cin >> n >> k;
    }

    return 0;
}